CREATE TABLE Account(
    applicationNO int primary key,
    accOpeningDate date,
    cdblParticipantId int,
    boId int,
    nameOfAc varchar(30),
    title varchar(30),
    accountType varchar(20)
);